agent_id: gemini-cli
runtime: Gemini CLI
status: planned-adapter
entrypoints:
  registry: agents/registry.yaml
contracts:
  - protocols/agent-context-routing.md
  - protocols/context-loading.md
notes:
  - At the current stage, keep only the adapter manifest and do not assume Gemini CLI automatically reads this directory.
