# Claude Code Memory

Claude Code runtime adaptation experience lives in this directory.

## Current Records

- Claude Code uses `CLAUDE.md`, `.claude/settings.json`, `.claude/skills/`, `.claude/agents/`, and `.mcp.json`.
