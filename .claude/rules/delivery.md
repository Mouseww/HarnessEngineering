# Delivery Rules

- Multi-step tasks require a plan.
- Delivery notes must include change scope, verification commands, and uncovered risks.
- Do not describe unverified status as complete.
- Do not run git commit, push, reset, or branch operations unless the user requested them.
- Reusable experience goes to `memory/`; team knowledge goes to `wiki/`; task status goes to `work/`.
