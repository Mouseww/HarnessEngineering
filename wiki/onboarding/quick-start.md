# Onboarding Quick Start

1. Read `README.md`.
2. Read the current agent entry: Claude Code reads `CLAUDE.md`; Codex reads `AGENTS.md`.
3. Run `scripts/doctor.ps1`.
4. Review `flows/feature-development.md`.
5. Write new tasks to `work/active/`.
